fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
pymupdf>=1.24.0
Pillow>=10.0.0
google-genai>=0.8.0
openai>=1.30.0
anthropic>=0.28.0
pandas>=2.2.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
aiofiles>=23.2.0
httpx>=0.27.0

# Scientific computing
scipy>=1.13.0
numpy>=1.26.0

# Rule-based extraction engine — fuzzy lexicon matching (cheese/ingredient/indicator
# name variants against the alias lists in app/extraction/lexicons/*.yml)
rapidfuzz>=3.6.0

# Async job queue (M9 production hardening)
celery>=5.4.0
redis>=5.0.0

# Export formats
pyarrow>=16.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Docling PDF extraction pipeline
docling>=2.14.0,<3.0
# Pinned explicitly: docling's own rapidocr/docling-ibm-models dependencies pull this
# in transitively with loose version bounds, which has already let pip resolve an
# incompatible 5.x build once (docling-ibm-models actually requires <5.0.0.0) and left
# cv2 in a broken half-installed state. Keep this pin if either dependency's bounds change.
opencv-python>=4.6.0.66,<5.0.0.0
